import type { Lang } from "./lang";
interface Params {
    username?: string;
    password?: string;
    baseUrl?: string;
    lang: Lang;
}
export declare const inquireParams: ({ username, password, baseUrl, lang, }: Params) => Promise<{
    username: string;
    password: string;
    baseUrl: string;
}>;
export {};
